{
  "name": "canvas",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/* --outdir=build/",
    "watch": "esbuild --watch src/* --outdir=build/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "alive-server": "^1.3.0",
    "esbuild": "^0.21.5"
  }
}
